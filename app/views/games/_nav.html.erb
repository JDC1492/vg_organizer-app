<% if user_signed_in? %>
<div class="footer">
    <h4><%= link_to "Edit Game", edit_user_game_path(current_user, @game) %></h4>
    <h4><%= link_to "Delete Game", @game, method: :delete, data:{confirm: "Hold your Horses! Do you really want to delete this game?"} %></h4>
    <h4><%= link_to "Back", user_games_path(current_user) %></h4>
</div>
<% end %>